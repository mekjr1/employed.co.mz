*.sublime-workspace
*settings.json
README-private.md

npm-debug.log
node_modules

# Env files — never commit secrets (STANDARDS.md §4)
.env
.env.*
!.env.example
.DS_Store
/**/.DS_Store
/**/.DS_Store

meteor.log
.claude
docs/claudePlans/**

# UAT / Playwright test outputs and caches
uat-artifacts/
.playwright-mcp/
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/e2e-results.json
tests/e2e/full-results.json

# A10.0 — perf scaffolding
build/
.lighthouse/

# Quality artefacts — git-tracked, reused across runs
# quality/ is NOT ignored (tracked in repo)

# Quality run results — ephemeral per-run data, not committed
.quality-run/
