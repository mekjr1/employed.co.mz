# Meteor packages used by this project, one per line.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

# A9.32 — LESS compiler removed after full CSS migration. All .less
# files have been converted to .css equivalents.
#less
service-configuration@1.3.0
accounts-ui@1.4.2
accounts-password@2.3.1
#accounts-facebook@1.3.3
accounts-github@1.5.0
accounts-google@1.4.0
#accounts-twitter@1.5.0
#accounts-meteor-developer@1.5.0
standard-minifier-css@1.8.1
standard-minifier-js@2.8.0
email@2.2.1
meteor-base@1.5.1
mobile-experience@1.1.0
mongo@1.14.6
blaze-html-templates@1.0.4
session@1.2.0
jquery@1.11.10
tracker@1.2.0
logging@1.3.1
reload@1.3.1
random@1.2.0
ejson@1.1.2
spacebars@1.0.12
check@1.3.1
# S9: `force-ssl` is deprecated in Meteor 3 and only enforces HTTPS at
# the app process. Production should terminate TLS at the edge (Cloudflare /
# load balancer) and rely on the HSTS + Strict-Transport-Security headers
# emitted from server/security-headers.js. Re-enable temporarily if you
# ever run the Meteor process as the public-facing TLS terminator.
#force-ssl@1.1.0
http
reactive-var@1.0.11

iron:router
raix:handlebar-helpers
momentjs:moment@=2.15.1
aldeed:autoform
aldeed:collection2
#multiply:iron-router-progress
# pauli:accounts-linkedin
natestrauser:connection-banner
lampe:rssfeed
aldeed:delete-button
# A9.32 (PR 2 BS5 migration) — replaced by hand-rolled Blaze
# templates in client/views/account/auth.{html,js} wired through
# router.js to the canonical Accounts.* API. The package's hard-coded
# Bootstrap-3 markup and its server-side
# `Accounts.config({ forbidClientAccountCreation: true })` gate were
# both blockers for the BS5 migration.
#useraccounts:bootstrap
meteorhacks:subs-manager
dburles:collection-helpers
alanning:roles
nemo64:bootstrap
zimme:iron-router-active
percolate:migrations
utilities:avatar
#matb33:collection-hooks
percolatestudio:synced-cron
reywood:publish-composite
nimble:restivus
# A9.32 — bootstrap-3-modal provides Modal.show/Modal.hide used by
# dialog.js, jobDeactivate, featuredCheckout, and userProfile. Keep
# until those are migrated to native BS5 Modal API.
peppelg:bootstrap-3-modal
mpowaga:autoform-summernote
djedi:sanitize-html
gadicohen:sitemaps
natestrauser:uploadcare-plus
#yogiben:admin
ongoworks:speakingurl
#staringatlights:infinite-scroll
# A9.32 — removed alongside useraccounts:bootstrap; the new auth
# routes are registered directly in router.js.
#useraccounts:iron-routing
ostrio:iron-router-title
#astronomerio:core
#shwaydogg:space-monkey
# B2.7: replaced by npm `stripe` (see package.json) + server/stripe.js +
# server/stripe-webhook.js. The old package wraps the deprecated Charges
# API in Fibers and predates SCA / 3-D Secure.
#copleykj:stripe-sync
mdg:seo
natestrauser:publish-performant-counts
shell-server@0.5.0
facebook-config-ui@1.0.3
github-config-ui@1.0.2
google-config-ui@1.0.3
meteor-developer-config-ui@1.0.2
twitter-config-ui@1.0.1
dynamic-import@0.7.2
underscore@1.0.10
ddp-rate-limiter

# S8 — CSP / framing policy (see server/security-headers.js).
browser-policy-content
browser-policy-framing

# S11 / A9.1 — Test driver. CI (.github/workflows/ci.yml) runs
#   meteor npm test
# Suites live under tests/. Was commented out before A9.1 which meant
# the CI test step was theater (no driver = nothing to run).
meteortesting:mocha
ecmascript
